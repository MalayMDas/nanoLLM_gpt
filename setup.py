"""
Setup script for the GPT project package.
This minimal setup.py is kept for backward compatibility.
The project is now configured via pyproject.toml.
"""

from setuptools import setup

# Minimal setup() call for backward compatibility
# All configuration is now in pyproject.toml
setup()
